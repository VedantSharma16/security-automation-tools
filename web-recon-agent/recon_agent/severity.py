"""Shared severity vocabulary used across findings, scoring, and narration."""

ORDER = ["critical", "high", "medium", "low", "info"]

WEIGHTS = {"critical": 40, "high": 25, "medium": 12, "low": 4, "info": 0}
